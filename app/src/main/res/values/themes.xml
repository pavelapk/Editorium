<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Editorium" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ColorBackgroundButton</item>
        <item name="colorPrimaryVariant">@color/ColorBackgroundBtnDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/ColorBackgroundF</item>
        <item name="android:colorBackground">@color/ColorBackgroundF</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ColorBackgroundButton</item>
        <item name="colorSecondaryVariant">@color/ColorBackgroundBtnDark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>
</resources>